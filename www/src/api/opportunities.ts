//

// import { createApiClient } from "api";
// //

// export const client = createApiClient(import.meta.env.VITE_STRAPI_API_URL);

//

export default async function handler(request: Request, context: any) {
  return;
}
