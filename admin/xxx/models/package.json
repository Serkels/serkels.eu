{
  "name": "@1/models",
  "version": "0.0.0",
  "main": "./index.js",
  "private": true,
  "scripts": {
    "dev": "pnpm run build --watch --preserveWatchOutput",
    "build:tsc": "tsc",
    "build": "pnpm run build:tsc"
  },
  "dependencies": {
    "@1/core": "workspace:*"
  },
  "devDependencies": {
    "@1/typescript-config": "workspace:*",
    "prettier": "3.0.2",
    "typescript": "5.1.6"
  }
}
